# common / cloud-base / network

This creates the base environment into which we'll create all our sample projects

## Create stack

```bash
./cloudops-apply.sh <ENV>
```

## Destroy stack

```bash
./cloudops-destroy.sh <ENV>
```
